api_version: duckdb_api/v2
kind: connector
id: retry_demo
version: 2.0.0
extractor_dialect: duckdb_api/json_path_v1

credentials: []

network_policy:
  origins:
    - scheme: https
      host: api.example.com
      port: 8443
  redirects: deny
  private_addresses: deny
  link_local_addresses: deny
  loopback_addresses: deny
  max_response_bytes: 1048576

relations:
  - duplicate_events
  - duplicate_graphql_events
