#pragma once

namespace duckdb_api_test {

// Runs the closed pagination declaration, operation-binding, scoped-resource,
// and safe-explanation laws independently of the native GitHub inventory.
void RunConnectorPaginationContractTests();

} // namespace duckdb_api_test
